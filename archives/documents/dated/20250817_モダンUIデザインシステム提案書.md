# HaQei Triple OS Analyzer モダンUIデザインシステム提案書
作成日: 2025年8月17日

## 🎨 デザインコンセプト

### テーマ: "Clarity & Intelligence"
- **ミニマル**: 余計な装飾を排除し、コンテンツに集中
- **プロフェッショナル**: 信頼感のある分析ツール
- **モダン**: 2025年のトレンドを反映
- **アクセシブル**: 誰もが使いやすい

## 🎨 カラーパレット

### プライマリカラー
```css
--primary-50: #f0f9ff;   /* 最も薄い青 */
--primary-100: #e0f2fe;
--primary-200: #bae6fd;
--primary-300: #7dd3fc;
--primary-400: #38bdf8;
--primary-500: #0ea5e9;  /* メインの青 */
--primary-600: #0284c7;
--primary-700: #0369a1;
--primary-800: #075985;
--primary-900: #0c4a6e;  /* 最も濃い青 */
```

### ニュートラルカラー
```css
--neutral-50: #fafafa;
--neutral-100: #f4f4f5;
--neutral-200: #e4e4e7;
--neutral-300: #d4d4d8;
--neutral-400: #a1a1aa;
--neutral-500: #71717a;
--neutral-600: #52525b;
--neutral-700: #3f3f46;
--neutral-800: #27272a;
--neutral-900: #18181b;
```

### アクセントカラー（Triple OS用）
```css
/* Engine OS - エネルギッシュな青緑 */
--engine-color: #06b6d4;    /* cyan-500 */
--engine-light: #ecfeff;    /* cyan-50 */

/* Interface OS - 温かみのある紫 */
--interface-color: #8b5cf6;  /* violet-500 */
--interface-light: #f3e8ff;  /* violet-50 */

/* Safe Mode OS - 安定感のある緑 */
--safe-color: #10b981;       /* emerald-500 */
--safe-light: #ecfdf5;       /* emerald-50 */
```

## 📐 レイアウトシステム

### グリッドシステム
```css
/* 12カラムグリッド、最大幅1280px */
.container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 24px;
}

@media (min-width: 768px) {
    .container {
        padding: 0 32px;
    }
}

@media (min-width: 1024px) {
    .container {
        padding: 0 48px;
    }
}
```

### スペーシングシステム
```css
/* 8pxベースのスペーシング */
--spacing-1: 8px;
--spacing-2: 16px;
--spacing-3: 24px;
--spacing-4: 32px;
--spacing-5: 40px;
--spacing-6: 48px;
--spacing-8: 64px;
--spacing-10: 80px;
```

## 🎯 コンポーネントデザイン

### 1. ウェルカム画面
```css
/* 背景 - シンプルなグラデーション */
body {
    background: linear-gradient(180deg, #fafafa 0%, #f4f4f5 100%);
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
}

/* ウェルカムカード */
.welcome-screen {
    background: white;
    border-radius: 16px;
    box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 
                0 1px 2px -1px rgb(0 0 0 / 0.1);
    padding: 48px;
    max-width: 600px;
    margin: 80px auto;
}

/* タイトル - グラデーションテキスト */
.welcome-screen h1 {
    font-size: 48px;
    font-weight: 800;
    background: linear-gradient(135deg, #0ea5e9 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 24px;
    letter-spacing: -0.02em;
}

/* サブテキスト */
.welcome-screen p {
    font-size: 18px;
    color: #71717a;
    line-height: 1.7;
    margin-bottom: 32px;
}

/* CTAボタン - フラットデザイン */
.primary-button {
    background: #0ea5e9;
    color: white;
    border: none;
    padding: 16px 32px;
    font-size: 16px;
    font-weight: 600;
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
    box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05);
}

.primary-button:hover {
    background: #0284c7;
    transform: translateY(-1px);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
}

.primary-button:active {
    transform: translateY(0);
}
```

### 2. 質問画面
```css
/* 質問カード */
.question-screen {
    background: white;
    border-radius: 16px;
    padding: 40px;
    max-width: 720px;
    margin: 40px auto;
    box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1);
}

/* プログレスバー - 細くてエレガント */
.progress-container {
    height: 4px;
    background: #e4e4e7;
    border-radius: 2px;
    margin-bottom: 40px;
    overflow: hidden;
}

.progress-bar {
    height: 100%;
    background: linear-gradient(90deg, #0ea5e9 0%, #8b5cf6 100%);
    border-radius: 2px;
    transition: width 0.5s cubic-bezier(0.4, 0, 0.2, 1);
}

/* 質問番号バッジ */
.question-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 16px;
    background: #f0f9ff;
    color: #0369a1;
    border-radius: 24px;
    font-size: 14px;
    font-weight: 600;
    margin-bottom: 24px;
}

/* 質問テキスト */
.question-text {
    font-size: 24px;
    font-weight: 600;
    color: #18181b;
    line-height: 1.4;
    margin-bottom: 32px;
}

/* 選択肢 - カード型 */
.option-card {
    display: flex;
    align-items: center;
    padding: 20px 24px;
    background: #fafafa;
    border: 2px solid #e4e4e7;
    border-radius: 12px;
    margin-bottom: 12px;
    cursor: pointer;
    transition: all 0.2s;
}

.option-card:hover {
    background: #f4f4f5;
    border-color: #d4d4d8;
    transform: translateX(4px);
}

/* ラジオボタン - カスタムデザイン */
.option-card input[type="radio"] {
    appearance: none;
    width: 20px;
    height: 20px;
    border: 2px solid #d4d4d8;
    border-radius: 50%;
    margin-right: 16px;
    position: relative;
    transition: all 0.2s;
}

.option-card input[type="radio"]:checked {
    border-color: #0ea5e9;
    background: #0ea5e9;
}

.option-card input[type="radio"]:checked::after {
    content: '';
    position: absolute;
    width: 8px;
    height: 8px;
    background: white;
    border-radius: 50%;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.option-card.selected {
    background: #f0f9ff;
    border-color: #0ea5e9;
}

/* ナビゲーションボタン */
.nav-buttons {
    display: flex;
    justify-content: space-between;
    margin-top: 40px;
    gap: 16px;
}

.nav-button {
    padding: 12px 24px;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.2s;
    cursor: pointer;
}

.nav-button.secondary {
    background: white;
    color: #52525b;
    border: 1px solid #e4e4e7;
}

.nav-button.secondary:hover {
    background: #fafafa;
    border-color: #d4d4d8;
}

.nav-button.primary {
    background: #0ea5e9;
    color: white;
    border: none;
}

.nav-button.primary:hover:not(:disabled) {
    background: #0284c7;
}

.nav-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
}
```

### 3. 結果画面
```css
/* 結果カード */
.results-screen {
    max-width: 1024px;
    margin: 40px auto;
    padding: 0 24px;
}

/* OSカード - グラスモーフィズム */
.os-card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 16px;
    padding: 32px;
    margin-bottom: 24px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
}

.os-card.engine {
    border-top: 4px solid #06b6d4;
}

.os-card.interface {
    border-top: 4px solid #8b5cf6;
}

.os-card.safe {
    border-top: 4px solid #10b981;
}

/* OSタイトル */
.os-title {
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 16px;
    display: flex;
    align-items: center;
    gap: 12px;
}

.os-icon {
    width: 40px;
    height: 40px;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 20px;
}

.os-icon.engine {
    background: #ecfeff;
    color: #06b6d4;
}

.os-icon.interface {
    background: #f3e8ff;
    color: #8b5cf6;
}

.os-icon.safe {
    background: #ecfdf5;
    color: #10b981;
}

/* スコアビジュアライゼーション */
.score-bar {
    height: 8px;
    background: #f4f4f5;
    border-radius: 4px;
    overflow: hidden;
    margin: 16px 0;
}

.score-fill {
    height: 100%;
    border-radius: 4px;
    transition: width 1s cubic-bezier(0.4, 0, 0.2, 1);
}

.score-fill.engine {
    background: linear-gradient(90deg, #06b6d4, #0891b2);
}

.score-fill.interface {
    background: linear-gradient(90deg, #8b5cf6, #7c3aed);
}

.score-fill.safe {
    background: linear-gradient(90deg, #10b981, #059669);
}
```

## 🎯 アニメーション

### マイクロインタラクション
```css
/* フェードイン */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* スライドイン */
@keyframes slideIn {
    from {
        transform: translateX(-20px);
        opacity: 0;
    }
    to {
        transform: translateX(0);
        opacity: 1;
    }
}

/* 要素の表示アニメーション */
.animate-in {
    animation: fadeIn 0.5s ease-out;
}

.animate-slide {
    animation: slideIn 0.3s ease-out;
}
```

## 📱 レスポンシブデザイン

### ブレークポイント
```css
/* モバイル: 320px - 767px */
/* タブレット: 768px - 1023px */
/* デスクトップ: 1024px+ */

@media (max-width: 768px) {
    .welcome-screen h1 {
        font-size: 32px;
    }
    
    .question-text {
        font-size: 20px;
    }
    
    .os-card {
        padding: 24px;
    }
}
```

## 🎨 デザイントークン

```css
:root {
    /* Colors */
    --color-primary: #0ea5e9;
    --color-primary-dark: #0284c7;
    --color-secondary: #8b5cf6;
    --color-success: #10b981;
    --color-warning: #f59e0b;
    --color-error: #ef4444;
    
    /* Typography */
    --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --font-size-xs: 12px;
    --font-size-sm: 14px;
    --font-size-base: 16px;
    --font-size-lg: 18px;
    --font-size-xl: 20px;
    --font-size-2xl: 24px;
    --font-size-3xl: 30px;
    --font-size-4xl: 36px;
    --font-size-5xl: 48px;
    
    /* Shadows */
    --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    --shadow-base: 0 1px 3px 0 rgb(0 0 0 / 0.1);
    --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
    --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
    
    /* Border Radius */
    --radius-sm: 4px;
    --radius-base: 8px;
    --radius-lg: 12px;
    --radius-xl: 16px;
    --radius-full: 9999px;
}
```

## 🎯 実装優先度

1. **Phase 1**: カラーパレットとタイポグラフィ
2. **Phase 2**: コンポーネントスタイル
3. **Phase 3**: アニメーション
4. **Phase 4**: レスポンシブ対応

---

このデザインシステムは、Figmaの最新トレンド、Linear、Vercel、Stripeなどのモダンなデザインを参考にしています。クリーンでプロフェッショナル、そして使いやすいインターフェースを実現します。